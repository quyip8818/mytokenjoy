# P1 问题修复计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复三个 P1 级别问题

**Architecture:** 后端 domain 层修改，三个修复互相独立可并行。

**Tech Stack:** Go 1.24, testutil pattern

## Global Constraints

- 测试使用 `testutil.NewTestStore(t)` + `testutil.Ctx()`
- 运行: `cd apps/backend && go test -tags=testhook ./tests/domain/org/... -v`
- Commit message: `fix(org): <description>`

---

### Task 1: SyncConfig 输入验证 — 阻止负阈值和无效频率

**Files:**
- Modify: `apps/backend/internal/domain/org/remote/sync.go:174-181`
- Create: `apps/backend/tests/domain/org/sync_config_test.go`

**修复:** 在 `UpdateSyncConfig` 中添加验证逻辑。

- [ ] **Step 1: 在 UpdateSyncConfig 开头添加验证**

```go
func (s *Service) UpdateSyncConfig(ctx context.Context, cfg types.SyncConfig) error {
	// Validate sync config inputs
	if cfg.FrequencyHours < 1 {
		return domain.Validation("frequencyHours must be at least 1")
	}
	if cfg.DeleteMemberThreshold < 0 {
		return domain.Validation("deleteMemberThreshold must not be negative")
	}
	if cfg.DeleteDepartmentThreshold < 0 {
		return domain.Validation("deleteDepartmentThreshold must not be negative")
	}

	integration, err := s.d.Store.Org().Integration(ctx)
	if err != nil {
		return err
	}
	integration.ApplySyncConfig(cfg)
	return s.d.Store.Org().SetIntegration(ctx, integration)
}
```

- [ ] **Step 2: 编写测试**

测试 3 个负向场景 + 1 个正向场景。

- [ ] **Step 3: 运行测试验证**
- [ ] **Step 4: Commit**

---

### Task 2: RemoveRoleMember 阻止移除最后一个超级管理员

**Files:**
- Modify: `apps/backend/internal/domain/org/structure/role.go:190-237`
- Create: `apps/backend/tests/domain/org/role_remove_last_admin_test.go`

**修复:** 在 RemoveRoleMember 中，当目标角色为超级管理员时，计算当前持有该角色的成员数，如果仅剩 1 人则拒绝。

- [ ] **Step 1: 在 RemoveRoleMember 的 role.Name == permission.RoleMember 检查后添加**

```go
	if role.Name == permission.RoleMember {
		return domain.NewDomainError(400, "Cannot remove base member role")
	}

	// Prevent removing the last super admin
	if role.Name == permission.RoleSuperAdmin {
		adminCount := 0
		for _, m := range members {
			if pkgorg.ContainsRole(m.Roles, permission.RoleSuperAdmin) {
				adminCount++
			}
		}
		if adminCount <= 1 {
			return domain.NewDomainError(400, "Cannot remove the last super admin")
		}
	}
```

- [ ] **Step 2: 编写测试**

- 测试只剩 1 个超级管理员时 RemoveRoleMember 返回错误
- 测试有 2 个超级管理员时可以正常移除其中一个

- [ ] **Step 3: 运行测试验证**
- [ ] **Step 4: Commit**

---

### Task 3: CreateRole 角色名重复检查

**Files:**
- Modify: `apps/backend/internal/domain/org/structure/role.go:19-40`
- Create: `apps/backend/tests/domain/org/role_create_duplicate_test.go`

**修复:** 在 CreateRole 中遍历已有角色，如果存在同名则返回 400。

- [ ] **Step 1: 在 CreateRole 的 NormalizeGrantIDs 之前添加名称检查**

```go
func (s *Local) CreateRole(ctx context.Context, name string, permissions []string) (types.Role, error) {
	roles, err := s.d.Store.Org().Roles(ctx)
	if err != nil {
		return types.Role{}, err
	}

	// Check for duplicate role name
	trimmedName := strings.TrimSpace(name)
	if trimmedName == "" {
		return types.Role{}, domain.Validation("role name must not be empty")
	}
	for _, existing := range roles {
		if existing.Name == trimmedName {
			return types.Role{}, domain.NewDomainError(400, "role name already exists")
		}
	}

	grantIDs, err := permission.NormalizeGrantIDs(permissions)
	...
```

- [ ] **Step 2: 编写测试**

- 测试重复名称被拒绝
- 测试空名称被拒绝
- 测试正常创建成功

- [ ] **Step 3: 运行测试验证**
- [ ] **Step 4: Commit**
