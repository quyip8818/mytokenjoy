# P0 问题修复计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复三个 P0 级别安全/数据完整性问题

**Architecture:** 修改后端 domain 层逻辑，前端无需改动。三个修复互相独立，可并行执行。

**Tech Stack:** Go 1.24, chi router, 内存 store + testutil

## Global Constraints

- 所有修复必须有单元测试覆盖
- 测试使用 `testutil.NewMemoryStoreFromConfig(t)` + `testutil.Ctx()`
- 测试文件在 `tests/domain/org/` 下
- 运行命令: `cd apps/backend && go test -tags=testhook ./tests/domain/org/... -v`
- Commit message 格式: `fix(org): <description>`

---

### Task 1: 修复成员更新全量替换导致数据丢失

**Files:**
- Modify: `apps/backend/internal/domain/org/structure/member.go:109-135`
- Modify: `apps/backend/internal/http/handler/org/member.go:33-42`
- Create: `apps/backend/tests/domain/org/member_update_test.go`

**Interfaces:**
- Consumes: `store.Store.Org().Members(ctx)`, `store.Store.Org().SetMembers(ctx, members)`
- Produces: `UpdateMember(ctx, id, input)` 现在执行 merge 而非替换

**问题根因:**
- `UpdateMember` 做 `updated := input; members[i] = updated`（全量替换）
- 前端只发送 name/phone/email/departmentId/departmentName
- 结果：Roles、Status、Source、CompanyID、PersonalQuota 全部被清零

**修复方案:** 后端 UpdateMember 改为 merge 语义——仅覆盖请求中非零值字段，保留未发送字段的原始值。

- [ ] **Step 1: 编写失败测试**

```go
// apps/backend/tests/domain/org/member_update_test.go
package org_test

import (
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"github.com/tokenjoy/backend/internal/domain/org/structure"
	"github.com/tokenjoy/backend/internal/domain/types"
	"github.com/tokenjoy/backend/tests/testutil"
)

func TestUpdateMember_PartialUpdate_PreservesRolesAndStatus(t *testing.T) {
	st := testutil.NewMemoryStoreFromConfig(t)
	ctx := testutil.Ctx()
	svc := structure.NewLocal(structure.Deps{Store: st})

	// Seed a member with roles and status
	members := []types.Member{{
		ID: "m-1", Name: "Alice", Phone: "111",
		Email: "alice@test.com", DepartmentID: "dept-1",
		Status: "active", Roles: []string{"普通成员", "财务"},
		Source: "manual", CompanyID: 1,
	}}
	require.NoError(t, st.Org().SetMembers(ctx, members))

	// Partial update: only change name and phone
	updated, err := svc.UpdateMember(ctx, "m-1", types.Member{
		Name:  "Alice Updated",
		Phone: "222",
	})

	require.NoError(t, err)
	assert.Equal(t, "Alice Updated", updated.Name)
	assert.Equal(t, "222", updated.Phone)
	// These must be preserved
	assert.Equal(t, "active", updated.Status)
	assert.Equal(t, []string{"普通成员", "财务"}, updated.Roles)
	assert.Equal(t, "manual", updated.Source)
	assert.Equal(t, int64(1), updated.CompanyID)
	assert.Equal(t, "alice@test.com", updated.Email) // not sent, preserved
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd apps/backend && go test -tags=testhook ./tests/domain/org/... -run TestUpdateMember_PartialUpdate -v
```

Expected: FAIL — Status 和 Roles 被清零

- [ ] **Step 3: 实现 merge 逻辑**

修改 `apps/backend/internal/domain/org/structure/member.go` 的 `UpdateMember` 函数：

```go
func (s *Local) UpdateMember(ctx context.Context, id string, input types.Member) (types.Member, error) {
	if err := validateRolesNotEscalated(input.Roles); err != nil {
		return types.Member{}, err
	}
	members, err := s.d.Store.Org().Members(ctx)
	if err != nil {
		return types.Member{}, err
	}
	for i := range members {
		if members[i].ID == id {
			existing := members[i]
			// Merge: only overwrite non-zero fields from input
			if input.Name != "" {
				existing.Name = input.Name
			}
			if input.Phone != "" {
				existing.Phone = input.Phone
			}
			if input.Email != "" {
				existing.Email = input.Email
			}
			if input.DepartmentID != "" {
				existing.DepartmentID = input.DepartmentID
				existing.DepartmentName = input.DepartmentName
			}
			if len(input.Roles) > 0 {
				rolesChanged := !slices.Equal(existing.Roles, input.Roles)
				existing.Roles = input.Roles
				if rolesChanged {
					if err := core.BumpAuthzRevision(ctx, s.d); err != nil {
						return types.Member{}, err
					}
				}
			}
			if input.Status != "" {
				existing.Status = input.Status
			}

			members[i] = existing
			if err := s.d.Store.Org().SetMembers(ctx, members); err != nil {
				return types.Member{}, err
			}
			return existing, nil
		}
	}
	return types.Member{}, domain.NewDomainError(404, "types.Member not found")
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd apps/backend && go test -tags=testhook ./tests/domain/org/... -run TestUpdateMember_PartialUpdate -v
```

Expected: PASS

- [ ] **Step 5: 运行全量 org 测试确保无回归**

```bash
cd apps/backend && go test -tags=testhook ./tests/... -v
```

Expected: All PASS

- [ ] **Step 6: Commit**

```bash
git add apps/backend/internal/domain/org/structure/member.go apps/backend/tests/domain/org/member_update_test.go
git commit -m "fix(org): use merge semantics in UpdateMember to prevent data loss

Previously UpdateMember did a full struct replacement, causing partial
updates from the frontend to clear Roles, Status, Source, and CompanyID.
Now only non-zero fields from the input are applied to the existing member."
```

---

### Task 2: AddRoleMember 添加预设角色保护

**Files:**
- Modify: `apps/backend/internal/domain/org/structure/role.go:143-178`
- Create: `apps/backend/tests/domain/org/role_add_member_test.go`

**Interfaces:**
- Consumes: `protectedRoles` map (from member.go), `permission.RoleSuperAdmin`, `permission.RoleOrgAdmin`
- Produces: `AddRoleMember` 现在拒绝向预设保护角色添加成员

**问题根因:**
- `validateRolesNotEscalated` 只在 `UpdateMember` 中调用
- `AddRoleMember` 无任何检查，允许向超级管理员角色添加任意成员

**修复方案:** 在 `AddRoleMember` 中检查目标角色是否为 preset 且为受保护角色，如是则返回 Forbidden。

- [ ] **Step 1: 编写失败测试**

```go
// apps/backend/tests/domain/org/role_add_member_test.go
package org_test

import (
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"github.com/tokenjoy/backend/internal/domain/org/structure"
	"github.com/tokenjoy/backend/internal/domain/types"
	"github.com/tokenjoy/backend/internal/infra/permission"
	"github.com/tokenjoy/backend/tests/testutil"
)

func TestAddRoleMember_RejectsProtectedRole(t *testing.T) {
	st := testutil.NewMemoryStoreFromConfig(t)
	ctx := testutil.Ctx()
	svc := structure.NewLocal(structure.Deps{Store: st})

	// Seed roles including super admin (preset)
	roles := []types.Role{
		{ID: "role-admin", Name: permission.RoleSuperAdmin, Type: "preset", Permissions: []string{"*"}},
		{ID: "role-custom", Name: "财务", Type: "custom", Permissions: []string{"budget:read"}},
	}
	require.NoError(t, st.Org().SetRoles(ctx, roles))

	members := []types.Member{
		{ID: "m-1", Name: "Bob", Roles: []string{"普通成员"}, Status: "active"},
	}
	require.NoError(t, st.Org().SetMembers(ctx, members))

	// Should reject adding to protected role
	err := svc.AddRoleMember(ctx, "role-admin", "m-1")
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "protected")

	// Should allow adding to custom role
	err = svc.AddRoleMember(ctx, "role-custom", "m-1")
	assert.NoError(t, err)
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd apps/backend && go test -tags=testhook ./tests/domain/org/... -run TestAddRoleMember_RejectsProtectedRole -v
```

Expected: FAIL — no error returned for protected role

- [ ] **Step 3: 添加保护检查**

在 `apps/backend/internal/domain/org/structure/role.go` 的 `AddRoleMember` 函数中，在找到 role 之后添加检查：

```go
func (s *Local) AddRoleMember(ctx context.Context, roleID, memberID string) error {
	roles, err := s.d.Store.Org().Roles(ctx)
	if err != nil {
		return err
	}
	members, err := s.d.Store.Org().Members(ctx)
	if err != nil {
		return err
	}

	var role *types.Role
	for i := range roles {
		if roles[i].ID == roleID {
			role = &roles[i]
			break
		}
	}
	if role == nil {
		return domain.NewDomainError(404, "Not found")
	}

	// Prevent adding members to protected preset roles via this endpoint
	if role.Type == "preset" {
		if _, protected := protectedRoles[role.Name]; protected {
			return domain.Forbidden("cannot assign protected role directly")
		}
	}

	for i := range members {
		if members[i].ID != memberID {
			continue
		}
		if !pkgorg.ContainsRole(members[i].Roles, role.Name) {
			members[i].Roles = append(members[i].Roles, role.Name)
			if err := s.d.Store.Org().SetMembers(ctx, members); err != nil {
				return err
			}
			return core.BumpAuthzRevision(ctx, s.d)
		}
		break
	}
	return nil
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd apps/backend && go test -tags=testhook ./tests/domain/org/... -run TestAddRoleMember_RejectsProtectedRole -v
```

Expected: PASS

- [ ] **Step 5: 运行全量测试**

```bash
cd apps/backend && go test -tags=testhook ./tests/... -v
```

- [ ] **Step 6: Commit**

```bash
git add apps/backend/internal/domain/org/structure/role.go apps/backend/tests/domain/org/role_add_member_test.go
git commit -m "fix(org): prevent role escalation via AddRoleMember

AddRoleMember now rejects attempts to add members to protected preset
roles (超级管理员, 组织管理员). Previously only UpdateMember had this
check, allowing a bypass through the role-member endpoint."
```

---

### Task 3: UpdateRole 阻止修改预设角色

**Files:**
- Modify: `apps/backend/internal/domain/org/structure/role.go:42-65`
- Create: `apps/backend/tests/domain/org/role_update_test.go`

**Interfaces:**
- Consumes: `types.Role.Type` field
- Produces: `UpdateRole` 现在拒绝修改 preset 类型角色

**问题根因:**
- `DeleteRole` 检查了 `role.Type == "preset"` 并拒绝
- `UpdateRole` 无此检查，允许修改超级管理员角色的权限列表

**修复方案:** 在 `UpdateRole` 中添加与 `DeleteRole` 一致的 preset 检查。

- [ ] **Step 1: 编写失败测试**

```go
// apps/backend/tests/domain/org/role_update_test.go
package org_test

import (
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
	"github.com/tokenjoy/backend/internal/domain/org/structure"
	"github.com/tokenjoy/backend/internal/domain/types"
	"github.com/tokenjoy/backend/internal/infra/permission"
	"github.com/tokenjoy/backend/tests/testutil"
)

func TestUpdateRole_RejectsPresetRole(t *testing.T) {
	st := testutil.NewMemoryStoreFromConfig(t)
	ctx := testutil.Ctx()
	svc := structure.NewLocal(structure.Deps{Store: st})

	roles := []types.Role{
		{ID: "role-admin", Name: permission.RoleSuperAdmin, Type: "preset", Permissions: []string{"*"}},
		{ID: "role-custom", Name: "财务", Type: "custom", Permissions: []string{"budget:read"}},
	}
	require.NoError(t, st.Org().SetRoles(ctx, roles))

	// Should reject modifying preset role
	_, err := svc.UpdateRole(ctx, "role-admin", "Hacked Admin", []string{"budget:read"})
	assert.Error(t, err)
	assert.Contains(t, err.Error(), "preset")

	// Should allow modifying custom role
	updated, err := svc.UpdateRole(ctx, "role-custom", "Finance", []string{"budget:read", "budget:write"})
	assert.NoError(t, err)
	assert.Equal(t, "Finance", updated.Name)
}
```

- [ ] **Step 2: 运行测试验证失败**

```bash
cd apps/backend && go test -tags=testhook ./tests/domain/org/... -run TestUpdateRole_RejectsPresetRole -v
```

Expected: FAIL — preset role gets updated without error

- [ ] **Step 3: 添加 preset 检查**

修改 `apps/backend/internal/domain/org/structure/role.go` 的 `UpdateRole`：

```go
func (s *Local) UpdateRole(ctx context.Context, id, name string, permissions []string) (types.Role, error) {
	roles, err := s.d.Store.Org().Roles(ctx)
	if err != nil {
		return types.Role{}, err
	}
	for i := range roles {
		if roles[i].ID == id {
			if roles[i].Type == "preset" {
				return types.Role{}, domain.NewDomainError(400, "Cannot modify preset role")
			}
			grantIDs, err := permission.NormalizeGrantIDs(permissions)
			if err != nil {
				return types.Role{}, domain.NewDomainError(400, err.Error())
			}
			roles[i].Name = name
			roles[i].Permissions = grantIDs
			if err := s.d.Store.Org().SetRoles(ctx, roles); err != nil {
				return types.Role{}, err
			}
			if err := core.BumpAuthzRevision(ctx, s.d); err != nil {
				return types.Role{}, err
			}
			return roles[i], nil
		}
	}
	return types.Role{}, domain.NewDomainError(404, "Not found")
}
```

- [ ] **Step 4: 运行测试验证通过**

```bash
cd apps/backend && go test -tags=testhook ./tests/domain/org/... -run TestUpdateRole_RejectsPresetRole -v
```

Expected: PASS

- [ ] **Step 5: 运行全量测试**

```bash
cd apps/backend && go test -tags=testhook ./tests/... -v
```

- [ ] **Step 6: Commit**

```bash
git add apps/backend/internal/domain/org/structure/role.go apps/backend/tests/domain/org/role_update_test.go
git commit -m "fix(org): block modification of preset roles in UpdateRole

UpdateRole now returns 400 for preset roles, consistent with DeleteRole.
Previously, a user with OrgRoles permission could modify the permissions
of preset roles like super admin, enabling privilege escalation."
```

---

## 执行策略

三个 Task 完全独立，修改不同的函数，可并行分配给 3 个 subagent：
- **Subagent 1**: Task 1 (member update merge)
- **Subagent 2**: Task 2 (AddRoleMember protection)
- **Subagent 3**: Task 3 (UpdateRole preset guard)

每个 subagent 完成后运行全量测试确认无回归，最后统一推送。
