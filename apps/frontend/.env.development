# Defaults for standalone `pnpm -F @tokenjoy/frontend start` (Local mode).
# Overridden by start-local.sh / start-saas.sh via env exports.
VITE_API_PROXY_TARGET=http://localhost:8011
VITE_DEV_PORT=9192
