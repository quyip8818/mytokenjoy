{
  "name": "@tokenjoy/backend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "DATABASE_URL=postgres://tokenjoy:tokenjoy@127.0.0.1:5432/tokenjoy?sslmode=disable make dev",
    "build": "make build",
    "build:check": "go build -o /dev/null ./...",
    "lint": "make lint",
    "format": "make format",
    "test": "make test-unit",
    "test:integration": "make test-integration"
  }
}
