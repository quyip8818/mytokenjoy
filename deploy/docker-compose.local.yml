# 本地开发 override: 暴露端口到宿主机，跳过 nginx
services:
  apps-frontend:
    ports:
      - '8080:80'

  sms-frontend:
    ports:
      - '8081:80'

  web:
    ports:
      - '8082:80'

  newapi:
    ports:
      - '3000:3000'
